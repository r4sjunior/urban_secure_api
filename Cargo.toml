[package]
name = "urban_secure_api"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
actix-cors = "0.7"
actix-files = "0.6"

serde = { version = "1", features = ["derive"] }
base64 = "0.22"

uuid = { version = "1", features = ["v4"] }